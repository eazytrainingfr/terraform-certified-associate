terraform {
  required_version = ">= 1.6"
  required_providers {
    gridscale = {
      source = "gridscale/gridscale" # https://registry.terraform.io/providers/gridscale/gridscale/latest
    }
  }
}